// Copy text to clipboard
function copyToClipboard(element) {
    var $temp = $("<input>");
    $("body").append($temp);
    $temp.val($(element).text()).select();
    document.execCommand("copy");
    $temp.remove();
  }
  // add this to element you want to copy to clipboard: onclick="copyToClipboard('#elementId')